name: "[DISABLED] Post Ready TikTok Content"

# DISABLED 2026-03-15: This workflow posted hardcoded images to TikTok
# without proper image validation. TikTok posts now go through the
# social-morning agent with full image pipeline validation.
# See: social-morning.yml

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "This workflow is DISABLED."
          echo "TikTok posting is handled by social-morning.yml with proper image validation."
          echo "Do NOT re-enable post_ready_tiktok.py — it posts hardcoded images without checks."
          exit 0
